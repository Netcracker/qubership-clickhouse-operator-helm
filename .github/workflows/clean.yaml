name: Remove Artifacts for deleted branch
on: delete

env:
  TAG_NAME: ${{ github.event.ref_name }}

jobs:
  delete:
    if: github.event.ref_type == 'branch'
    permissions:
      packages: write
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        component:
          - name: qubership-clickhouse-258
            context: ""
          - name: qubership-clickhouse-243
            context: ""
          - name: qubership-clickhouse-integration-tests
            context: ""
          - name: qubership-clickhouse-transfer
            context: ""
          - name: qubership-clickhouse-hook
            context: ""
          - name: qubership-clickhouse-site-manager
            context: ""
          - name: qubership-clickhouse-secret-monitor
            context: ""
          - name: qubership-docker-clickhouse-backup-sidecar
            context: ""
          - name: qubership-docker-clickhouse-dbaas-adapter
            context: ""
          - name: qubership-docker-clickhouse-backup-orchestrator
            context: ""
    steps:
      - name: Prepare Tag
        run: echo "TAG_NAME=$(echo ${TAG_NAME} | sed 's@refs/heads/@@;s@/@_@g')" >> $GITHUB_ENV
      - name: Login to Docker Hub
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          registry: ghcr.io
          username: ${GITHUB_ACTOR}
          password: ${{secrets.GITHUB_TOKEN}}
      - name: Get package IDs for delete
        id: get-ids-for-delete
        uses: Netcracker/get-package-ids@84bc8eb8bed50218be76e671b3a24c35a1300979
        with:
          component-name: ${{ matrix.component.name }}
          component-tag: ${{ env.TAG_NAME }}
          access-token: ${{secrets.GITHUB_TOKEN}}
      - uses: actions/delete-package-versions@v5
        with:
          package-name: ${{ matrix.component.name }}
          package-type: 'container'
          package-version-ids: ${{ steps.get-ids-for-delete.outputs.ids-for-delete }}
        if: ${{ steps.get-ids-for-delete.outputs.ids-for-delete != '' }}