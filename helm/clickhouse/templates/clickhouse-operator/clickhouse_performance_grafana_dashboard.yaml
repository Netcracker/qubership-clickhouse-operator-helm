{{ if (eq (include "monitoring.install" .) "true") }}
apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  name: clickhouse-performance-grafana-dashboard
  labels:
    name: clickhouse-performance-grafana-dashboard
    {{ include "kubernetes.labels" . | nindent 4 }}
    app: grafana
spec:
  gzipJson: {{ .Files.Get "monitoring/clickhouse_performance_grafana_dashboard.json.gz" | b64enc | quote }}
{{ end }}